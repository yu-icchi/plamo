<div class="checkbox">
    <h5>{{label}}</h5>
    {{#each options}}
        <label>
            <input type="checkbox" name="{{group}}[]" value="{{value}}">
            {{label}}
        </label>
    {{/each}}
</div>
