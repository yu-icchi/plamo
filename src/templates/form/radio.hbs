<div class="radio">
    <h5>{{label}}</h5>
    {{#each options}}
        <label>
            <input type="radio" name="{{group}}" id="{{key}}" value="{{value}}">
            {{label}}
        </label>
    {{/each}}
</div>
